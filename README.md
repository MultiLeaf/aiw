# AI Workflow

Portable spec-driven development workflow for AI coding agents.

## Development

```bash
npm install
npm run build
node dist/cli.js install --target codex
```

The project configuration lives in `.ai-workflow/`; target-specific files are generated by adapters.
